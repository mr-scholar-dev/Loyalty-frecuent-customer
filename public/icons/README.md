# PWA icons

Referenced by `public/manifest.webmanifest`. Add before enabling install/PWA
promotion (Phase 5):

- `icon-192.png` (192×192)
- `icon-512.png` (512×512)

Both should work with `purpose: "any maskable"` (keep important content within
the safe zone). Binary assets are intentionally not committed in Phase 0.
